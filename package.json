{
  "private": true,
  "name": "frost",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "init": "lerna bootstrap",
    "clean": "lerna exec -- npm run clean",
    "build": "lerna run build",
    "serve": "cd ./packages/backend && npm run serve",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
