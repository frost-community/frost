## tspのバージョン表示
```
pnpm exec tsp --version
```

## リビルド
```
pnpm run rebuild
```

以下のコマンドでgeneratedディレクトリ下に.yamlファイルをコピーする
```
pnpm run apply
```

## 依存関係のインストール
```
pnpm i
```
`tsp install`は使用しないこと！
